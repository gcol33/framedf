% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/frame.R
\name{framedf_settings}
\alias{framedf_settings}
\title{Settings for \code{frame()}}
\usage{
framedf_settings(adjustment = NULL, ...)
}
\arguments{
\item{adjustment}{Optional adjustment columns (passed through unchanged).}

\item{...}{Named overrides.}
}
\value{
A named list of settings.
}
\description{
Returns the list of tunable thresholds used by \code{\link[=frame]{frame()}}. Every field can
be overridden through the \code{...} argument of \code{frame()}. The defaults are
chosen to be conservative on small data and to scale to millions of rows
through progressive subsampling.
}
\section{Fields}{

\describe{
\item{\code{min_obs}}{Minimum complete cases required to screen a pair.}
\item{\code{strong_threshold}, \code{moderate_threshold}, \code{weak_threshold}}{
Absolute correlation cut-offs used to classify pair strength.}
\item{\code{near_constant_ratio}}{Maximum allowed share of the dominant value
before a column is flagged as near-constant.}
\item{\code{id_unique_ratio}}{If a character column has more than this share
of unique values, it is treated as an identifier.}
\item{\code{skew_threshold}}{Absolute skewness above which a column is
flagged as right- or left-skewed.}
\item{\code{subsample_threshold}}{Row count above which numeric pairs are
screened by progressive subsampling.}
\item{\code{subsample_probe}, \code{subsample_confirm}}{Probe and confirmation
sample sizes for the two-stage subsampling.}
\item{\code{compositional_cv}}{Maximum coefficient of variation of \code{x + y}
for the pair to be flagged as a constrained complement.}
\item{\code{observer_min_levels}}{Minimum number of categorical levels
before a strong group effect counts as an observer-style concern.}
\item{\code{min_level_n}}{Levels with fewer than this many rows are flagged
as rare.}
\item{\code{seed}}{Seed used by the subsampling layer.}
\item{\code{adjustment}}{Optional column names to partial out before
numeric–numeric screening.}
}
}

