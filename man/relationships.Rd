% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relationships.R
\name{relationships}
\alias{relationships}
\alias{relationships.frame_df}
\title{Show Relationships Found by \code{frame()}}
\usage{
relationships(x, ...)

\method{relationships}{frame_df}(x, min_strength = "weak", kind = NULL, ...)
}
\arguments{
\item{x}{A \code{frame_df} object.}

\item{...}{Additional arguments (unused).}

\item{min_strength}{Minimum strength to display: \code{"weak"}, \code{"moderate"},
or \code{"strong"}. Structural relationships are always shown.}

\item{kind}{Restrict to a single kind: \code{"meaningful"}, \code{"suspicious"},
\code{"structural"}, \code{"ignored"}, or \code{NULL} (all).}
}
\value{
The list of findings, invisibly.
}
\description{
Prints a structured listing of all relationship findings, grouped into
four kinds: meaningful, suspicious, structural, and ignored. Each pair
is described qualitatively (direction, strength, stability, method).
}
\details{
Numeric evidence (correlation coefficients, F-statistics, p-values) is
available on the returned object; the printed view is meant to be
skimmable.
}
\examples{
set.seed(1)
df <- data.frame(
  x = rnorm(100),
  y = rnorm(100)
)
df$z <- 0.8 * df$x + 0.2 * stats::rnorm(100)
relationships(frame(df))
}
